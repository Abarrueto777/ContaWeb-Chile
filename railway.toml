[build]
builder = "nixpacks"

[deploy]
startCommand = "cd apps/api && npx prisma migrate deploy && node dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
